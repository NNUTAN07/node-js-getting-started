version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing Node.js dependencies...
      - npm ci

  pre_build:
    commands:
      - echo Running tests...
      # - npm test

  build:
    commands:
      - echo Build phase complete.

artifacts:
  files:
    - '**/*'
    - '.*'
  discard-paths: yes
