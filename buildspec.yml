version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing Node.js dependencies...
      - npm ci

  pre_build:
    commands:
      - echo Running tests...
      # - npm test

  build:
    commands:
      - echo Zipping the application...
      - zip -r ../nodejs-app.zip * .[^.]* -x "node_modules/*" 
      - echo Uploading to S3 root...
      #- aws s3 cp ../nodejs-app.zip s3://nuthanpractice-bucket/

artifacts:
  files:
    - nodejs-app.zip
